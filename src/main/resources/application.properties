    spring.application.name=SelfCheckout
    server.port=8087
    server.address=0.0.0.0

    # H2 File Database
    spring.datasource.url=jdbc:h2:file:C:/Users/saurabh/test;AUTO_SERVER=TRUE
    spring.datasource.driver-class-name=org.h2.Driver
    spring.datasource.username=sa
    spring.datasource.password=

    # ? FIX 1: Tell Spring to always run data.sql on startup
    spring.sql.init.mode=always

    # ? FIX 2: Must be true so data.sql runs AFTER Hibernate creates tables
    spring.jpa.defer-datasource-initialization=true
spring.h2.console.settings.web-allow-others=true

    spring.jpa.hibernate.ddl-auto=update
    spring.jpa.show-sql=true
    spring.jpa.properties.hibernate.format_sql=true

    # H2 Console
    spring.h2.console.enabled=true
    spring.h2.console.path=/h2-console

    # JWT
    jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    jwt.expiration=86400000